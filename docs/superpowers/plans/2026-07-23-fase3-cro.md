# Fase 3 — CRO — Implementation Plan

> Eseguire con superpowers:executing-plans.

**Goal:** Migliorare la conversione: CTA "Affiliati" raggiungibile su mobile, percorsi chiari per i tre pubblici in home, gestione obiezioni con FAQ sull'affiliazione.

**Architecture:** Modifiche a componenti/pagine esistenti sullo stack Next.js. Nessuna nuova dipendenza.

**Tech Stack:** Next.js 16, React 19, TypeScript, Tailwind 4.

## Global Constraints

- Brand invariato (BRAND.md): oro `#eab308` CTA, blu `#1e40af` link/eyebrow, Geist.
- I file toccati NON devono introdurre `any` né errori di lint; dove tocco codice con `any` preesistente, lo sistemo.
- Nessun claim numerico inventato (le "50+ palestre" restano un open item separato).
- Verifica: `npm run build` + lint + controllo nel browser. Nessun test framework.

---

### Task 1: Header — CTA Affiliati su mobile, nav fix, tipizzazione

**Files:** Modify `src/components/Header.tsx`; Modify `src/app/page.tsx` (hero CTA → /discipline).

- [ ] Step 1: tipizzare la nav: `type NavSub = { name: string; href: string; row?: string }` e `type NavItem = { name: string; href: string; dropdown?: NavSub[] }`; annotare `const navigation: NavItem[]`.
- [ ] Step 2: sostituire i due `(subItem as any).row` con `subItem.row`.
- [ ] Step 3: nella nav, cambiare `{ name: 'Discipline', href: '/#discipline' }` → `href: '/discipline'`; rimuovere l'item `{ name: 'Omologazioni Titoli', href: '/gare#omologazioni' }`.
- [ ] Step 4: nel menu mobile (blocco "CTA Button at Bottom") aggiungere SOPRA il link Instagram un link primario oro a `/affiliazione` ("Affiliati Ora"), con `onClick` che chiude il menu. Instagram resta sotto come secondaria.
- [ ] Step 5: in `src/app/page.tsx` cambiare l'hero CTA "Le Nostre Discipline" da `/#discipline` a `/discipline`.
- [ ] Step 6: build + lint puliti; nel browser (mobile) il menu mostra "Affiliati Ora".
- [ ] Step 7: commit.

### Task 2: Home — segmentazione pubblico

**Files:** Modify `src/app/page.tsx`.

- [ ] Step 1: dopo la Stats Section e prima della Disciplines Section, aggiungere una `<section className="section">` "Percorsi" con 3 card (usare `.card`, `.eyebrow`), ciascuna con titolo, testo e link:
  - "Gestisci una palestra?" → "Affilia la tua palestra" → `/affiliazione`
  - "Vuoi allenarti?" → "Trova la sede più vicina" → `/palestre`
  - "Vuoi insegnare?" → "Diventa istruttore" → `/formazione`
- [ ] Step 2: griglia responsive (`grid sm:grid-cols-3 gap-6`), CTA come `Link` con classe `btn btn-secondary` o link testuale blu.
- [ ] Step 3: build + lint; controllo desktop/mobile nel browser.
- [ ] Step 4: commit.

### Task 3: FAQ affiliazione + FAQPage schema

**Files:** Modify `src/app/affiliazione/page.tsx`.

- [ ] Step 1: definire `const faqs: { q: string; a: string }[]` con domande basate sui contenuti reali (come funziona, cosa include, assicurazione, formazione istruttori, quote su richiesta). NIENTE prezzi inventati.
- [ ] Step 2: aggiungere una sezione FAQ (dopo il form, prima della CTA dark) con le domande/risposte (dettagli `<details>`/`<summary>` accessibili o lista con heading).
- [ ] Step 3: aggiungere JSON-LD `FAQPage` generato da `faqs` (script `application/ld+json`).
- [ ] Step 4: build + lint; verifica JSON-LD valido (presente nel sorgente).
- [ ] Step 5: commit.

## Self-Review

- Copertura: CTA mobile ✓ (T1), segmentazione ✓ (T2), gestione obiezioni/FAQ+schema ✓ (T3). Trust signals: la home ha già la Stats Section (titoli/atleti/discipline) sopra le sezioni di dettaglio → non si aggiungono claim nuovi.
- No placeholder; contenuti FAQ da rivedere col committente (segnalato) ma veritieri.
